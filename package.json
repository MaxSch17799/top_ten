{
  "name": "top-ten",
  "private": true,
  "type": "module",
  "workspaces": ["backend-do"],
  "scripts": {
    "sync:question-banks": "node scripts/sync-question-banks.mjs",
    "deploy:worker": "npm run sync:question-banks && cd backend-do && npx wrangler deploy"
  }
}
